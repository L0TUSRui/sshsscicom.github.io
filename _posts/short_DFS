#include <bits/stdc++.h>
using namespace std;
const int MN = 2e5;

int N,M;
vector<int> G[MN];

void proc(){}

void DFS(int a, int r)
{
  for(int x : G[a]){
    if(x==r) continue;
    DFS(x,a);
  }
  proc();
}

int main()
{
  cin >> N >> M;
  for(int i=0; i<M; i++){
    int A,B;
    cin >> A >> B;
    G[A].push_back(B);
    G[B].push_back(A);
  }
  DFS();
}
